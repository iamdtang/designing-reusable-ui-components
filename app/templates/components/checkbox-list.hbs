<ul>
  {{#each @items as |item|}}
    <li>
      {{yield
        item
        (component "checkbox"
          checked=(in-set this.checkedSet item)
          onCheck=(action "handleCheck" item))}}
    </li>
  {{/each}}
</ul>

{{!-- <Checkbox @checked={{true}} @onCheck={{action "doSomething"}} /> --}}
