{{checkedTeamMembers.length}} selected
<CheckboxList
  @items={{teamMembers}}
  @checked={{checkedTeamMembers}}
  @onCheck={{action "handleCheck"}} as |teamMember Checkbox|>
  <Checkbox class="my-custom-class" />
  {{teamMember.name}}
</CheckboxList>
