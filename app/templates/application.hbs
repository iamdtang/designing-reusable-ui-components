<CheckboxList
  @items={{teamMembers}}
  @checked={{checkedTeamMembers}}
  @onCheck={{action "handleCheck"}} as |item Checkbox|>
  <Checkbox class="my-custom-class" />
  {{item.name}}
</CheckboxList>
